# SPRAG 기반 사내 메신저 AI Assistant Architecture 문서

## 1. 문서 목적

본 문서는 사내 SW Architect 양성과정 팀 과제의 Architecture 정의를 위한 기초 문서이다. 대상 시스템은 사내 메신저에 통합되는 AI Assistant이며, 핵심 설계 대상은 AI Chatbot UI가 아니라 중복 자료가 많은 사내 데이터를 적은 토큰으로 정확하게 검색하고 활용하기 위한 SPRAG 기반 RAG Pipeline이다.

SPRAG은 중복 문서, 유사 문서, 반복 대화, 버전 차이가 있는 문서가 많은 원본 데이터에서 의미적으로 대표성 있는 근거를 추출하고 LLM 입력 토큰을 최적화하기 위한 신규 RAG 기법으로 가정한다. 본 문서에서는 SPRAG 내부 알고리즘을 블랙박스로 두고, 시스템 관점에서 SPRAG을 사용하는 Offline/Online Pipeline, 이해관계자, Use Case, 기능 요구사항, 품질속성을 정의한다.

## 2. 문제 정의

사내 업무 지식은 메신저 대화, Wiki/Confluence 문서, Jira 티켓, 회의록, 장애 대응 기록 등 여러 시스템에 분산되어 있다. 사용자는 업무 맥락을 이해하기 위해 여러 저장소를 직접 검색해야 하며, 검색 결과의 최신성, 정확성, 권한 적합성을 스스로 판단해야 한다.

기존 RAG 방식은 원본 데이터를 수집한 뒤 청킹, 임베딩, Vector 검색, LLM 답변 생성으로 이어지는 단순 Pipeline을 구성하는 경우가 많다. 그러나 사내 데이터는 동일하거나 유사한 내용이 여러 문서, 여러 버전, 여러 대화방에 반복 저장된다. 이러한 데이터를 그대로 인덱싱하면 다음 문제가 발생한다.

- 중복 청크 증가로 Vector DB 저장량과 검색 비용이 증가한다.
- 유사한 근거가 반복 검색되어 LLM 입력 토큰이 낭비된다.
- 오래된 문서와 최신 문서가 함께 검색되어 답변 일관성이 저하된다.
- 공식 문서와 비공식 대화가 충돌할 때 판단 기준이 불명확하다.
- 사용자가 접근할 수 없는 문서나 세션이 LLM 컨텍스트에 포함될 위험이 있다.
- Multi-turn, Multi-session 질문에서 현재 대화 맥락과 과거 세션 범위를 안정적으로 결정하기 어렵다.

따라서 대상 시스템은 단순 질의응답 기능을 넘어, 사내 지식 데이터의 중복성과 권한 제약을 고려한 RAG Pipeline Architecture를 제공해야 한다.

## 3. 프로젝트 배경

사내 업무는 메신저 중심으로 진행되지만, 업무 산출물은 Wiki/Confluence, Jira, 파일 저장소, 회의록 시스템 등으로 분산된다. 이로 인해 실제 업무 지식은 공식 문서와 비공식 대화에 나뉘어 존재한다. 공식 문서는 규정, 설계 결정, API 명세와 같은 기준 정보를 제공하지만, 실무 대화에는 장애 원인 분석, 임시 대응, 의사결정 배경, 아직 문서화되지 않은 최신 정보가 포함된다.

대부분의 조직에서는 시간이 지남에 따라 문서 중복이 증가한다. 유사 가이드 문서, 복사된 템플릿, 버전별 문서, 프로젝트별 파생 문서가 누적되고, 메신저에는 동일 이슈에 대한 반복 질의와 해결 과정이 남는다. 이 데이터가 정제되지 않은 상태로 RAG에 투입되면 검색 정확도와 비용 효율성이 동시에 악화된다.

또한 사내 AI Assistant는 일반 검색 서비스보다 높은 보안성과 추적성을 요구한다. 사용자가 볼 수 없는 문서, 개인 대화방, 보존 기간이 만료된 메시지, AI 참조가 비허용된 세션은 검색 결과와 LLM 컨텍스트에 포함되어서는 안 된다. 그룹 채팅에서는 참여자별 권한이 다르므로, 답변에 사용할 수 있는 정보 범위를 동적으로 계산해야 한다.

본 프로젝트는 이러한 문제를 해결하기 위해 SPRAG 기반 Offline Pipeline과 Online Retrieval Pipeline을 분리하여 설계한다. Offline Pipeline은 원본 데이터를 수집, 정규화, 중복 제거, 대표화, 메타데이터 부여, 인덱싱하는 역할을 수행한다. Online Pipeline은 사용자의 질의와 세션 맥락을 해석하고, 권한 정책을 적용한 뒤, 관련 근거를 검색, 리랭킹, 컨텍스트 조립하여 LLM에 전달한다.

## 4. Architecture Scope

### 4.1 In Scope

- 사내 메신저, Wiki/Confluence, Jira 등 원본 데이터 수집 구조
- SPRAG 기반 Offline 데이터 정제 및 인덱싱 Pipeline
- 중복/유사 데이터 처리를 위한 대표 청크 및 메타데이터 관리
- Multi-turn, Multi-session 질의를 위한 세션 컨텍스트 관리
- 사용자 및 그룹 채팅 참여자 권한 기반 검색 범위 결정
- Online Retrieval, Re-ranking, Top-K 선정, LLM Context 구성
- 답변 출처 제공 및 정보 충돌 시 판단 기준 정의
- ISO/IEC 25010 기준 주요 품질속성 및 NFR/QA 정의

### 4.2 Out of Scope

- SPRAG 내부 알고리즘의 세부 구현
- 실제 LLM 모델 학습 또는 파인튜닝
- 메신저 UI 상세 설계
- 운영 대시보드 상세 화면 설계
- 특정 Vector DB 또는 LLM Provider 제품 선정
- 실제 서비스 배포 및 운영 자동화 구현

## 5. Architecture Goals

| ID | Architecture Goal | 설명 |
| --- | --- | --- |
| AG-1 | 중복 데이터 최소화 | 중복/유사 원본 데이터를 Offline 단계에서 정제하여 저장량, 검색량, LLM 입력 토큰을 줄인다. |
| AG-2 | 근거 기반 답변 정확성 확보 | 질의와 관련성이 높은 근거를 선별하고 출처를 명시하여 답변 신뢰성을 높인다. |
| AG-3 | Multi-session Context 지원 | 현재 대화뿐 아니라 접근 가능한 과거 세션을 활용해 업무 맥락을 이어간다. |
| AG-4 | 권한 및 보존 정책 준수 | 사용자가 접근할 수 없는 정보와 참조 비허용 데이터는 검색 및 답변 생성에서 제외한다. |
| AG-5 | Offline/Online 책임 분리 | 고비용 데이터 정제 작업은 Offline으로 수행하고, Online 질의는 응답성과 안정성을 우선한다. |
| AG-6 | 정책 변경 용이성 확보 | 검색 가중치, Top-K, 출처 우선순위, TTL, 권한 정책을 설정 기반으로 조정 가능하게 한다. |

## 6. Stakeholders

| Stakeholder | 관심사 | Architecture 관점의 요구 |
| --- | --- | --- |
| 일반 임직원 | 업무 질문에 대해 빠르고 정확한 답변을 받고 싶다. | 현재 대화 맥락과 과거 세션을 활용한 정확한 답변, 출처 제공, 낮은 응답 지연 |
| 프로젝트/팀 리더 | 과거 의사결정, 장애 이력, 업무 배경을 재활용하고 싶다. | 프로젝트별 대화, 문서, 티켓을 연결한 맥락 검색과 요약 |
| 경영진/프로젝트 스폰서 | AI 도입 비용 대비 생산성 향상을 확인하고 싶다. | 토큰 사용량 절감, 중복 데이터 감소, 운영 비용 통제 |
| 보안/컴플라이언스 담당자 | 권한 없는 정보와 보존 기간 만료 데이터가 노출되지 않아야 한다. | ACL 기반 필터링, TTL 동기화, 감사 로그, 민감정보 마스킹 |
| 시스템 운영자 | 피크 시간과 장애 상황에서도 안정적으로 운영하고 싶다. | 확장 가능한 Retrieval 구조, 장애 격리, 재처리, 관측성 |
| 지식 관리자/문서 관리자 | 중복 문서, 오래된 문서, 출처 품질을 관리하고 싶다. | 문서 버전, 최신성, 대표 청크, 출처 신뢰도 메타데이터 |
| AI/플랫폼 개발자 | RAG Pipeline과 Prompt 정책을 지속적으로 개선하고 싶다. | 모듈화된 Pipeline, 설정 기반 정책, 테스트 가능한 검색/리랭킹 구조 |

## 7. Key Use Cases

### UC-1. 멀티 세션 업무 회고

사용자는 개인 AI 대화방에서 특정 프로젝트, 장애, 설계 변경, 의사결정에 대해 과거 여러 대화방과 관련 문서를 함께 검색해 달라고 요청한다. 시스템은 사용자가 접근 가능한 세션과 문서만 대상으로 검색하고, 관련 근거를 요약하여 답변한다.

### UC-2. 사내 문서 기반 질의응답

사용자는 사내 규정, API 명세, 운영 가이드, Jira 이슈 상태 등에 대해 질문한다. 시스템은 Wiki/Confluence, Jira 등 공식 데이터 원천을 검색하고, 최신성과 출처 신뢰도를 고려하여 답변한다.

### UC-3. 그룹 채팅 컨텍스트 지원

그룹 채팅에서 사용자가 현재 논의와 관련된 과거 정보나 문서를 요청한다. 시스템은 그룹 참여자들의 권한을 기준으로 검색 가능한 데이터 범위를 계산하고, 참여자에게 노출 가능한 근거만 사용한다.

### UC-4. 세션 참조 제어

사용자는 특정 대화방 또는 세션을 AI 참조 대상에서 제외하거나 다시 허용한다. 시스템은 해당 정책을 검색 메타데이터와 권한 필터링에 반영하여 이후 질의에서 해당 세션의 사용 여부를 제어한다.

### UC-5. 정보 충돌 판별

검색된 근거 중 공식 문서, Jira, 메신저 대화의 내용이 서로 다를 수 있다. 시스템은 출처 유형, 문서 상태, 최신성, 권한, 신뢰도 점수를 기준으로 우선순위를 결정하고, 상충 정보가 존재하는 경우 답변에 이를 명시한다.

### UC-6. SPRAG Offline 데이터 정제

시스템은 사내 원본 데이터를 주기적으로 수집하고, 중복/유사 데이터 탐지, 대표 청크 생성, 메타데이터 부여, 인덱싱을 수행한다. 이 과정은 Online 질의 응답 성능에 직접 영향을 주지 않도록 비동기적으로 수행된다.

## 8. Functional Requirements

| ID | 기능 요구사항 |
| --- | --- |
| FR-1 | 시스템은 사내 메신저, Wiki/Confluence, Jira 등 복수의 원본 데이터 소스로부터 데이터를 수집할 수 있어야 한다. |
| FR-2 | 시스템은 수집한 원본 데이터를 문서, 메시지, 티켓, 댓글 등 검색 가능한 단위로 정규화할 수 있어야 한다. |
| FR-3 | 시스템은 LLM 또는 Embedding 처리 전에 보안 정책에 따른 민감정보 마스킹을 수행할 수 있어야 한다. |
| FR-4 | 시스템은 SPRAG Offline Pipeline을 통해 중복/유사 데이터를 탐지하고 대표 청크 또는 압축 표현을 생성할 수 있어야 한다. |
| FR-5 | 시스템은 각 청크에 원본 시스템, 문서 유형, 작성자, 작성 시각, 버전, 권한, 세션, 최신성, 신뢰도 메타데이터를 부여해야 한다. |
| FR-6 | 시스템은 원본 데이터 변경, 삭제, 권한 변경 이벤트를 인덱스와 메타데이터 저장소에 반영할 수 있어야 한다. |
| FR-7 | 사용자는 현재 세션 맥락을 유지한 상태에서 Multi-turn 질의를 수행할 수 있어야 한다. |
| FR-8 | 사용자는 접근 권한이 있는 과거 여러 세션을 대상으로 Multi-session 질의를 수행할 수 있어야 한다. |
| FR-9 | 사용자는 특정 세션 또는 대화방의 AI 참조 허용 여부를 설정할 수 있어야 한다. |
| FR-10 | 시스템은 사용자 질의 시 사용자 권한, 세션 참조 정책, 그룹 참여자 권한을 기반으로 검색 범위를 결정해야 한다. |
| FR-11 | 시스템은 Vector 검색과 메타데이터 필터링을 결합하여 권한과 정책에 부합하는 후보 근거만 검색해야 한다. |
| FR-12 | 시스템은 검색된 후보 근거를 질의 관련도, 최신성, 출처 신뢰도 기준으로 Re-ranking할 수 있어야 한다. |
| FR-13 | 시스템은 최종 Top-K 근거만 LLM Context에 포함해야 한다. |
| FR-14 | 시스템은 답변 생성 시 사용된 주요 출처를 사용자에게 제공해야 한다. |
| FR-15 | 시스템은 상충되는 근거가 존재하는 경우, 판단 기준과 출처 차이를 답변에 명시할 수 있어야 한다. |
| FR-16 | 시스템은 질의, 검색 결과, 적용된 권한 정책, 답변 출처를 감사 및 분석 목적으로 기록할 수 있어야 한다. |

## 9. Non-Functional Requirements and Quality Attributes

품질속성은 ISO/IEC 25010의 Product Quality Model을 기준으로 정의한다. 각 NFR은 Architecture 설계 결정을 유도하는 품질속성 시나리오로 표현한다.

### 9.1 Functional Suitability

| 하위 특성 | NFR / QA |
| --- | --- |
| Functional Completeness | 시스템은 사내 문서 검색, 메신저 세션 검색, 권한 필터링, Re-ranking, LLM 답변 생성, 출처 제공의 핵심 흐름을 모두 지원해야 한다. |
| Functional Correctness | 시스템은 사용자가 접근 가능한 근거만 사용하여 답변해야 하며, 답변의 주요 주장에는 검색된 근거가 연결되어야 한다. |
| Functional Appropriateness | 시스템은 단순 키워드 검색 결과가 아니라 질의 의도, 세션 맥락, 출처 신뢰도를 반영한 근거를 선택해야 한다. |

### 9.2 Performance Efficiency

| 하위 특성 | NFR / QA |
| --- | --- |
| Time Behaviour | Online 질의 처리에서는 권한 계산, 검색, Re-ranking, LLM Context 조립이 사용자 경험을 저해하지 않는 목표 응답 시간 안에 수행되어야 한다. |
| Resource Utilization | SPRAG 적용 후 중복 청크 저장량, 검색 후보 수, LLM 입력 토큰 수는 기존 단순 RAG 대비 감소해야 한다. |
| Capacity | 피크 시간대 동시 질의 증가에 대응할 수 있도록 Retrieval, Re-ranking, LLM Gateway는 수평 확장 가능한 구조여야 한다. |

### 9.3 Compatibility

| 하위 특성 | NFR / QA |
| --- | --- |
| Co-existence | AI Assistant와 RAG Pipeline은 기존 사내 메신저 및 업무 시스템의 주요 트랜잭션 성능을 저하시키지 않아야 한다. |
| Interoperability | 시스템은 사내 메신저, Wiki/Confluence, Jira와 API, Webhook, Event Stream 등 표준화된 연동 방식으로 데이터를 교환할 수 있어야 한다. |

### 9.4 Usability

| 하위 특성 | NFR / QA |
| --- | --- |
| Appropriateness Recognizability | 사용자는 AI 답변이 어떤 문서나 세션을 근거로 생성되었는지 이해할 수 있어야 한다. |
| Operability | 사용자는 대화방 또는 세션 단위로 AI 참조 허용 여부를 쉽게 확인하고 변경할 수 있어야 한다. |
| User Error Protection | 사용자가 세션 참조를 비허용으로 설정한 경우, 시스템은 해당 설정이 이후 질의에 반영되었음을 명확히 처리해야 한다. |

### 9.5 Reliability

| 하위 특성 | NFR / QA |
| --- | --- |
| Maturity | Offline Pipeline의 일부 데이터 수집 실패나 중복 정제 실패가 Online 질의 서비스 전체 장애로 전파되지 않아야 한다. |
| Availability | Wiki/Confluence 또는 Jira 등 일부 원본 시스템이 일시적으로 장애 상태여도 기존 인덱스를 기반으로 제한된 답변을 제공할 수 있어야 한다. |
| Fault Tolerance | Indexing Worker, Metadata Sync Worker, Re-ranking Service 등 주요 구성요소 장애 시 재시도 또는 대체 경로가 존재해야 한다. |
| Recoverability | 실패한 수집, 정제, 인덱싱 작업은 재처리 가능해야 하며, 중복 처리로 인해 인덱스 정합성이 깨지지 않아야 한다. |

### 9.6 Security

| 하위 특성 | NFR / QA |
| --- | --- |
| Confidentiality | 사용자가 접근 권한이 없는 문서, 티켓, 메시지, 세션은 검색 결과와 LLM Context에 포함되어서는 안 된다. |
| Integrity | 원본 데이터, 메타데이터, 권한 정보, 인덱스 간 정합성이 유지되어야 하며 임의 변경이 방지되어야 한다. |
| Non-repudiation | 주요 질의, 답변, 출처, 권한 판단 결과는 사후 감사가 가능하도록 기록되어야 한다. |
| Accountability | 시스템은 특정 답변이 어떤 사용자, 질의, 권한 정책, 근거 데이터에 의해 생성되었는지 추적 가능해야 한다. |
| Authenticity | 사용자와 서비스 간 호출은 인증된 주체를 기준으로 수행되어야 하며, 원본 시스템 접근 권한은 검증 가능한 신원에 기반해야 한다. |

### 9.7 Maintainability

| 하위 특성 | NFR / QA |
| --- | --- |
| Modularity | Offline 수집/정제/인덱싱, Online 검색/Re-ranking/LLM 호출은 독립적으로 변경 가능한 모듈로 분리되어야 한다. |
| Reusability | 원본 데이터 Connector, Metadata Filter, Re-ranking 정책, Prompt Template은 다른 데이터 소스나 서비스에서도 재사용 가능해야 한다. |
| Analysability | 검색 실패, 부정확한 답변, 권한 필터링 오류의 원인을 추적할 수 있도록 로그와 메트릭을 제공해야 한다. |
| Modifiability | Top-K, 검색 가중치, 출처 우선순위, TTL, 권한 정책은 코드 변경 없이 설정으로 조정 가능해야 한다. |
| Testability | SPRAG Offline Pipeline, 권한 필터링, Re-ranking, Prompt 조립은 독립 테스트와 회귀 테스트가 가능해야 한다. |

### 9.8 Portability

| 하위 특성 | NFR / QA |
| --- | --- |
| Adaptability | 배포 환경, 데이터 소스, 조직별 권한 체계가 달라져도 설정과 Connector 변경을 통해 적용 가능해야 한다. |
| Installability | Offline Worker, Retrieval Service, LLM Gateway 등 주요 구성요소는 독립 배포가 가능해야 한다. |
| Replaceability | Vector DB, Embedding 모델, LLM Provider, Re-ranking 모델은 인터페이스 기반으로 교체 가능해야 한다. |

## 10. Key Quality Attribute Priorities

본 과제에서 우선적으로 다루어야 할 품질속성은 다음과 같다.

| 우선순위 | ISO/IEC 25010 품질특성 | 이유 |
| --- | --- | --- |
| 1 | Functional Suitability | SPRAG Pipeline의 목적은 정확하고 적절한 근거를 선택해 업무 질문에 맞는 답변을 제공하는 것이다. |
| 2 | Security | 사내 데이터 기반 AI Assistant는 권한 없는 정보 노출을 방지해야 하며, 그룹 채팅에서는 권한 정책이 핵심 설계 조건이다. |
| 3 | Performance Efficiency | SPRAG 도입의 핵심 동인은 중복 데이터와 LLM 입력 토큰을 줄여 비용과 응답시간을 통제하는 것이다. |
| 4 | Reliability | Offline 데이터 처리와 Online 질의 처리가 분리되어야 하며, 일부 장애가 전체 서비스 장애로 확산되지 않아야 한다. |
| 5 | Maintainability | RAG 정책, 권한 정책, 데이터 소스, LLM Provider는 지속적으로 변경될 수 있으므로 설정화와 모듈화가 필요하다. |

## 11. Architecture Assumptions

- SPRAG 내부 알고리즘은 블랙박스로 취급하며, 입력은 정규화된 원본 데이터와 메타데이터, 출력은 대표 청크 또는 압축된 검색 단위로 가정한다.
- 원본 시스템은 API, Webhook, Event Stream 중 하나 이상의 방식으로 데이터 변경 이벤트를 제공할 수 있다고 가정한다.
- 사용자의 사내 시스템 접근 권한은 기존 IAM 또는 각 원본 시스템의 권한 정보를 통해 검증 가능하다고 가정한다.
- LLM Provider는 외부 또는 내부 모델 모두 가능하며, 민감정보 마스킹과 권한 필터링 이후에만 Context를 전달한다.
- 본 문서는 Architecture 요구사항 정의까지를 범위로 하며, 상세 컴포넌트 설계와 배포 구조는 후속 문서에서 정의한다.
